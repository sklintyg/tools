apiVersion: v1
kind: BuildConfig
metadata:
  labels:
    app: rehabstod
    name: rehabstod-pipeline
    template: application-template-sample-pipeline
  name: rehabstod-pipeline
  namespace: intygstjanster-test
spec:
  nodeSelector: null
  output: {}
  postCommit: {}
  resources: {}
  runPolicy: Serial
  source:
    type: None
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfile: |-
        try {
           timeout(time: 20, unit: 'MINUTES') {
              node {
                  stage('build') {
                    openshiftBuild(buildConfig: 'rehabstod', showBuildLogs: 'true')
                  }
                }
           }
        } catch (err) {
           echo "in catch block"
           echo "Caught: ${err}"
           currentBuild.result = 'FAILURE'
           throw err
        }
    type: JenkinsPipeline
status:
  lastVersion: 0
